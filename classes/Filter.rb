class Filter
  # @param numbers [Array] must be an array
  # @return [Array] an array containing the filtered results
  # @return [Array] an empty array if no elements match the filter criteria
  def only_even(numbers)
    # TODO
  end

  # @param values [Array]
  # @return [Array] an array containing the filtered results
  # @return [Array] an empty array if no elements match the filter criteria
  def exclude_nil(values)
    # TODO
  end

end